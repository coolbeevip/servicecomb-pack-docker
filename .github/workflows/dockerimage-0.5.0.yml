name: 0.5.0

on: [push]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - name: Build the Docker image
      run: docker build -f 0.5.0/Dockerfile -t coolbeevip/servicecomb-pack .
